//when new FileAlterationMonitor(interval) ,interval is less，for example 10,
//then the file is change ,but onFileChange() can not be called.
FileAlterationMonitor fileAlterationMonitor = new FileAlterationMonitor(10);
